#to remove the spaces from the string to make it just alphanumeric. we can use regex [a-z0-9]
 
#approach 1: two pointer    Complexity =  time: O(n)   space:O(1)

#approch 2 : reverse the array and compare with original     Complexity = time:O(n)  space:O(n)